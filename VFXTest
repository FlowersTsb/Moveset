-- [[MOVES]]
local Players = game:GetService("Players")
local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

local playerGui = player:WaitForChild("PlayerGui")
local hotbar = playerGui:FindFirstChild("Hotbar")
local backpack = hotbar:FindFirstChild("Backpack")
local hotbarFrame = backpack:FindFirstChild("Hotbar")

-- Button 1
local baseButton1 = hotbarFrame:FindFirstChild("1").Base
local ToolName1 = baseButton1.ToolName
ToolName1.Text = "Black Flash"

-- Button 2
local baseButton2 = hotbarFrame:FindFirstChild("2").Base
local ToolName2 = baseButton2.ToolName
ToolName2.Text = "Move 2"

-- Button 3
local baseButton3 = hotbarFrame:FindFirstChild("3").Base
local ToolName3 = baseButton3.ToolName
ToolName3.Text = "Move 3"

-- Button 4
local baseButton4 = hotbarFrame:FindFirstChild("4").Base
local ToolName4 = baseButton4.ToolName
ToolName4.Text = "Move 4"

-- GUI Wait Function
local function waitForGui()
    local playerGui = player:WaitForChild("PlayerGui")
    
    while true do
        local screenGui = playerGui:FindFirstChild("ScreenGui")
        if screenGui then
            local magicHealthFrame = screenGui:FindFirstChild("MagicHealth")
            if magicHealthFrame then
                local textLabel = magicHealthFrame:FindFirstChild("TextLabel")
                if textLabel then
                    textLabel.Text = "ULT NAME HERE"
                    return
                end
            end
        end
        wait(1)
    end
end

waitForGui()

-- [[ANIMATIONS]]
local replacementAnimations = {
    ["10468665991"] = "rbxassetid://10468665991",
    ["Here"] = "rbxassetid://here",
    ["Here"] = "rbxassetid://here",
    ["Here"] = "rbxassetid://here",
    ["Here"] = "rbxassetid://here",
    ["Here"] = "rbxassetid://here",
    ["Here"] = "rbxassetid://here",
    ["Here"] = "rbxassetid://here",
    ["Here"] = "rbxassetid://here",
    ["Here"] = "rbxassetid://here",
    ["Here"] = "rbxassetid://here",
    ["Here"] = "rbxassetid://here",
    ["Here"] = "rbxassetid://here",
    ["Here"] = "rbxassetid://here"
}
-- [[VFX + MOVE SET]]
local function onAnimationPlayed(animationTrack)
    if player.Character and humanoid then
        local animationId = animationTrack.Animation.AnimationId:match("%d+")
        local replacementId = replacementAnimations[animationId]
      
        if animationId = "10468665991" then
            wait(1)
            
            -- [][VFX][]
            local Test = -- REMOVE THIS TEXT THEN PUT THE VFX COPY PATH 
            local test = Test:Clone()
            test.Parent = game.Players.LocalPlayer.Character["HumanoidRootPart"]

            for _, child in ipairs(test:GetChildren()) do
                if child:IsA("ParticleEmitter") then
                    child:Emit(10)
                    child.Enabled = true
                end
            end
            
            wait(2)
            test:Destroy()
        end
      -- VFX 2
      local function onAnimationPlayed(animationTrack)
    if player.Character and humanoid then
        local animationId = animationTrack.Animation.AnimationId:match("%d+")
        local replacementId = replacementAnimations[animationId]
      
        if animationId == "paste your first Animation id here" then
            wait(1)
            
            -- [][VFX][]
            local Test = -- REMOVE THIS TEXT THEN PUT THE VFX COPY PATH 
            local test = Test:Clone()
            test.Parent = game.Players.LocalPlayer.Character["HumanoidRootPart"]

            for _, child in ipairs(test:GetChildren()) do
                if child:IsA("ParticleEmitter") then
                    child:Emit(10)
                    child.Enabled = true
                end
            end
            
            wait(2)
            test:Destroy()
        end
      -- VFX 3
      local function onAnimationPlayed(animationTrack)
    if player.Character and humanoid then
        local animationId = animationTrack.Animation.AnimationId:match("%d+")
        local replacementId = replacementAnimations[animationId]
      
        if animationId == "paste your first Animation id here" then
            wait(1)
            
            -- [][VFX][]
            local Test = -- REMOVE THIS TEXT THEN PUT THE VFX COPY PATH 
            local test = Test:Clone()
            test.Parent = game.Players.LocalPlayer.Character["HumanoidRootPart"]

            for _, child in ipairs(test:GetChildren()) do
                if child:IsA("ParticleEmitter") then
                    child:Emit(10)
                    child.Enabled = true
                end
            end
            
            wait(2)
            test:Destroy()
        end
      -- VFX 4
      local function onAnimationPlayed(animationTrack)
    if player.Character and humanoid then
        local animationId = animationTrack.Animation.AnimationId:match("%d+")
        local replacementId = replacementAnimations[animationId]
      
        if animationId == "paste your first Animation id here" then
            wait(1)
            
            -- [][VFX][]
            local Test = -- REMOVE THIS TEXT THEN PUT THE VFX COPY PATH 
            local test = Test:Clone()
            test.Parent = game.Players.LocalPlayer.Character["HumanoidRootPart"]

            for _, child in ipairs(test:GetChildren()) do
                if child:IsA("ParticleEmitter") then
                    child:Emit(10)
                    child.Enabled = true
                end
            end
            
            wait(2)
            test:Destroy()
        end
      -- VFX 5
      local function onAnimationPlayed(animationTrack)
    if player.Character and humanoid then
        local animationId = animationTrack.Animation.AnimationId:match("%d+")
        local replacementId = replacementAnimations[animationId]
      
        if animationId == "paste your first Animation id here" then
            wait(1)
            
            -- [][VFX][]
            local Test = -- REMOVE THIS TEXT THEN PUT THE VFX COPY PATH 
            local test = Test:Clone()
            test.Parent = game.Players.LocalPlayer.Character["HumanoidRootPart"]

            for _, child in ipairs(test:GetChildren()) do
                if child:IsA("ParticleEmitter") then
                    child:Emit(10)
                    child.Enabled = true
                end
            end
            
            wait(2)
            test:Destroy()
        end
      -- VFX 5
local function onAnimationPlayed(animationTrack)
    if player.Character and humanoid then
        local animationId = animationTrack.Animation.AnimationId:match("%d+")
        local replacementId = replacementAnimations[animationId]
      
        if animationId == "paste your first Animation id here" then
            wait(1)
            
            -- [][VFX][]
            local Test = -- REMOVE THIS TEXT THEN PUT THE VFX COPY PATH 
            local test = Test:Clone()
            test.Parent = game.Players.LocalPlayer.Character["HumanoidRootPart"]

            for _, child in ipairs(test:GetChildren()) do
                if child:IsA("ParticleEmitter") then
                    child:Emit(10)
                    child.Enabled = true
                end
            end
            
            wait(2)
            test:Destroy()
        end
      -- VFX 6
      local function onAnimationPlayed(animationTrack)
    if player.Character and humanoid then
        local animationId = animationTrack.Animation.AnimationId:match("%d+")
        local replacementId = replacementAnimations[animationId]
      
        if animationId == "paste your first Animation id here" then
            wait(1)
            
            -- [][VFX][]
            local Test = -- REMOVE THIS TEXT THEN PUT THE VFX COPY PATH 
            local test = Test:Clone()
            test.Parent = game.Players.LocalPlayer.Character["HumanoidRootPart"]

            for _, child in ipairs(test:GetChildren()) do
                if child:IsA("ParticleEmitter") then
                    child:Emit(10)
                    child.Enabled = true
                end
            end
            
            wait(2)
            test:Destroy()
        end
      -- VFX 7
      local function onAnimationPlayed(animationTrack)
    if player.Character and humanoid then
        local animationId = animationTrack.Animation.AnimationId:match("%d+")
        local replacementId = replacementAnimations[animationId]
      
        if animationId == "paste your first Animation id here" then
            wait(1)
            
            -- [][VFX][]
            local Test = -- REMOVE THIS TEXT THEN PUT THE VFX COPY PATH 
            local test = Test:Clone()
            test.Parent = game.Players.LocalPlayer.Character["HumanoidRootPart"]

            for _, child in ipairs(test:GetChildren()) do
                if child:IsA("ParticleEmitter") then
                    child:Emit(10)
                    child.Enabled = true
                end
            end
            
            wait(2)
            test:Destroy()
        end
      -- VFX 8
      local function onAnimationPlayed(animationTrack)
    if player.Character and humanoid then
        local animationId = animationTrack.Animation.AnimationId:match("%d+")
        local replacementId = replacementAnimations[animationId]
      
        if animationId == "paste your first Animation id here" then
            wait(1)
            
            -- [][VFX][]
            local Test = -- REMOVE THIS TEXT THEN PUT THE VFX COPY PATH 
            local test = Test:Clone()
            test.Parent = game.Players.LocalPlayer.Character["HumanoidRootPart"]

            for _, child in ipairs(test:GetChildren()) do
                if child:IsA("ParticleEmitter") then
                    child:Emit(10)
                    child.Enabled = true
                end
            end
            
            wait(2)
            test:Destroy()
        end
      -- VFX 9
      local function onAnimationPlayed(animationTrack)
    if player.Character and humanoid then
        local animationId = animationTrack.Animation.AnimationId:match("%d+")
        local replacementId = replacementAnimations[animationId]
      
        if animationId == "paste your first Animation id here" then
            wait(1)
            
            -- [][VFX][]
            local Test = -- REMOVE THIS TEXT THEN PUT THE VFX COPY PATH 
            local test = Test:Clone()
            test.Parent = game.Players.LocalPlayer.Character["HumanoidRootPart"]

            for _, child in ipairs(test:GetChildren()) do
                if child:IsA("ParticleEmitter") then
                    child:Emit(10)
                    child.Enabled = true
                end
            end
            
            wait(2)
            test:Destroy()
        end
      -- VFX 10
      local function onAnimationPlayed(animationTrack)
    if player.Character and humanoid then
        local animationId = animationTrack.Animation.AnimationId:match("%d+")
        local replacementId = replacementAnimations[animationId]
      
        if animationId == "paste your Animation id here" then
            wait(1)
            
            -- [][VFX][]
            local Test = -- REMOVE THIS TEXT THEN PUT THE VFX COPY PATH 
            local test = Test:Clone()
            test.Parent = game.Players.LocalPlayer.Character["HumanoidRootPart"]

            for _, child in ipairs(test:GetChildren()) do
                if child:IsA("ParticleEmitter") then
                    child:Emit(10)
                    child.Enabled = true
                end
            end
            
            wait(2)
            test:Destroy()
        end
      
        -- [[ULT MOVES NAMES MAKE SURE TO PASTE YOUR ULT ANIMATION ID]]
        if animationId == "paste your ULT Animation id here" then
            wait(2)

            -- Names for Ultimate Moves
            local toolNames = {
                {buttonIndex = "1", name = "Ult move 1"},
                {buttonIndex = "2", name = "Ult move 2"},
                {buttonIndex = "3", name = "Ult move 3"},
                {buttonIndex = "4", name = "Ult move 4"},
            }
          
            for _, tool in ipairs(toolNames) do
                local baseButton = hotbarFrame:FindFirstChild(tool.buttonIndex).Base
                baseButton.ToolName.Text = tool.name
            end
        end

        
        if replacementId then
            animationTrack:Stop()
            local newAnimation = Instance.new("Animation")
            newAnimation.AnimationId = replacementId
            local newTrack = humanoid:LoadAnimation(newAnimation)
            newTrack:Play()
        end
    end
end

humanoid.AnimationPlayed:Connect(onAnimationPlayed)

player.CharacterAdded:Connect(function(newCharacter)
    character = newCharacter
    humanoid = newCharacter:WaitForChild("Humanoid")
    humanoid.AnimationPlayed:Connect(onAnimationPlayed)
end)
